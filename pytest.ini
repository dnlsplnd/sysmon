[pytest]
# The package is not installed into the environment under test -- `make install`
# puts it under a prefix, deliberately outside site-packages -- so the repo root
# goes on sys.path here instead.
pythonpath = .
testpaths = tests
addopts = -q
